<% buttons = Spree::HtmlInvoice::Config[:print_buttons] || "packaging_slip"
   buttons = buttons.split(",").collect{|b| b.strip } %>
<%  buttons.each do |button| %>
<%= button_link_to(t(button.to_s+".print"), "/shop/admin/invoice/#{@order.number}/#{button}" , :target => "_blank") %>
<%end%>
